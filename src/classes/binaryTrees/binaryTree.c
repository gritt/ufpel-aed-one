#include "binaryTree.h"

Tree * initializeTree()
{
    Tree *thisTree =  malloc(sizeof(Tree));
    
    thisTree->size = 0;
    thisTree->rootNode = 0;
    
    return thisTree;
}


void insertInTree(Tree *thisTree)
{
    printf("\nits here\n");
}

