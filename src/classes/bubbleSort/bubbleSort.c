#include <stdio.h>
#include <stdlib.h>

int main()
{
    int rows, arrayToBeSort[];
    
    //read how many rows have to read
    scanf("%d", &rows);
    
    
    //allocate space for all inputs
    if (### == NULL) {
        printf("Impossible to allocate space");
        exit(0);
    }
    
    arrayToBeSort[] = malloc(rows * sizeof(int))
    
    return 0;
}


int bubbleSort()
{

}